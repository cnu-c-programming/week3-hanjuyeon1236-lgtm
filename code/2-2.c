#include <stdio.h>

void swap(int *x, int *y) {

}

int main() {
  int x = 100, y = 200;

  printf("x = %d, y = %d\n", x, y);

  swap();
  printf("x = %d, y = %d\n", x, y);

  reurn 0;
}
